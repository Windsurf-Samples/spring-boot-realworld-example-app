<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.spring.infrastructure.mybatis.mapper.UserMapper">
    <insert id="insert">
        insert into users (id, username, password, email, bio, image)
        values (#{user.id}, #{user.username}, #{user.password}, #{user.email}, #{user.bio}, #{user.image})
    </insert>

    <select id="findByUsername" resultType="io.spring.core.user.User">
        select * from users where username = #{username}
    </select>

    <select id="findByEmail" resultType="io.spring.core.user.User">
        select * from users where email = #{email}
    </select>

    <select id="findById" resultType="io.spring.core.user.User">
        select * from users where id = #{id}
    </select>

    <update id="update">
        update users
        set username = #{user.username},
            password = #{user.password},
            email = #{user.email},
            bio = #{user.bio},
            image = #{user.image}
        where id = #{user.id}
    </update>

    <select id="findRelation" resultType="io.spring.core.user.FollowRelation">
        select user_id as userId, follow_id as targetId from follows where user_id = #{userId} and follow_id = #{targetId}
    </select>

    <insert id="saveRelation">
        insert into follows (user_id, follow_id) values (#{followRelation.userId}, #{followRelation.targetId})
    </insert>

    <delete id="deleteRelation">
        delete from follows where user_id = #{followRelation.userId} and follow_id = #{followRelation.targetId}
    </delete>
</mapper>
