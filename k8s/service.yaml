apiVersion: v1
kind: Service
metadata:
  name: articles-api-service
  namespace: default
  labels:
    app: articles-api
spec:
  selector:
    app: articles-api
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
